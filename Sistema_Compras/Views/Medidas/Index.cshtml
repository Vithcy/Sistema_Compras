@model IEnumerable<Sistema_Compras.Medidas>

@{
    ViewBag.Title = "Index";
}

<h2>Medidas</h2>

<p>
    @Html.ActionLink("Crear Medidas", "Create")
</p>

@* La barra de busqueda Inicia *@
<form class="form-inline active-pink-3 active-pink-4" method="get">
    <input class="form-control form-control-sm ml-3 w-75" type="text" id="Criterio" name="Criterio" />
    <input class="btn btn-info" type="submit" id="cmdBuscar" value="Buscar" />
</form>
@* La barra de busqueda Fin *@

<table class="table table-striped table-dark">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Unidad_de_Medida)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Activo)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Unidad_de_Medida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activo)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.IdMedida }) |
                @Html.ActionLink("Detalles", "Details", new { id = item.IdMedida }) |
                @Html.ActionLink("Borrar", "Delete", new { id = item.IdMedida })
            </td>
        </tr>
    }

</table>
