@model IEnumerable<Sistema_Compras.Proveedores>

@{
    ViewBag.Title = "Index";
}

<h2>Proveedores</h2>

<p>
    @Html.ActionLink("Crear Proveedor", "Create")
</p>


@* La barra de busqueda Inicia *@
<form class="form-inline active-pink-3 active-pink-4" method="get">
    <input class="form-control form-control-sm ml-3 w-75" type="text" id="Criterio" name="Criterio" />
    <input class="btn btn-info" type="submit" id="cmdBuscar" value="Buscar" />
</form>
@* La barra de busqueda Fin *@

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cedula_o_RNC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Comercial)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Activo)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cedula_o_RNC)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_Comercial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activo)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.IdProv }) |
                @Html.ActionLink("Detalles", "Details", new { id = item.IdProv }) |
                @Html.ActionLink("Borrar", "Delete", new { id = item.IdProv })
            </td>
        </tr>
    }

</table>
